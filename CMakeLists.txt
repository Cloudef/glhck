CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(ROOT)
SET(PROJECT_NAME "GLhck Framework")
SET(GLUE_DESCRIPTION "A dead simple OpenGL/GLES framework")

SET(LIB_OUT_DIR ${CMAKE_BINARY_DIR}/build/lib)
SET(TEST_OUT_DIR ${CMAKE_BINARY_DIR}/build/test)
SET(INCLUDE_DIR ${CMAKE_BINARY_DIR}/include)
include_directories(${INCLUDE_DIR})

# Turns on/off GLEW linking on tests
SET(USE_GLES 0)

IF(PANDORA)
    ADD_DEFINITIONS(-DPANDORA)
    SET(USE_GLES 1)
ENDIF(PANDORA)

ADD_SUBDIRECTORY(lib)
ADD_SUBDIRECTORY(test)
