SET(TEST_EXE
   display)

SET(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${TEST_OUT_DIR})
FOREACH (test ${TEST_EXE})
   PROJECT(${test})
   ADD_EXECUTABLE(${test} src/${test}.c)
   INCLUDE_DIRECTORIES(${GLHCK_TEST_INCL})
   TARGET_LINK_LIBRARIES(${test} ${GLHCK_TEST_LINK})
ENDFOREACH (test)
