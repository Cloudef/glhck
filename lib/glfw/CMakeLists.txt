SET(GLFW_INC
   src/include/GL/glfw3.h)

SET(GLFW_BUILD_EXAMPLES 0)
SET(GLFW_BUILD_TESTS 0)
SET(GLFW_USE_EGL 0)
IF (GLHCK_USE_GLES)
   SET(GLFW_USE_EGL 1)
ENDIF ()

ADD_SUBDIRECTORY(src)
FILE(COPY ${GLFW_INC} DESTINATION ${INCLUDE_DIR}/GL)
