PROJECT(actc)

SET(ACTC_SRC
   src/tc.c)

SET(ACTC_INC
   src/tc.h)

FILE(COPY ${ACTC_INC} DESTINATION ${GLHCK_INCLUDE_DIR})
FILE(COPY ${ACTC_INC} DESTINATION ${INCLUDE_DIR})
ADD_DEFINITIONS(-fPIC)
ADD_LIBRARY(actc ${ACTC_SRC})
TARGET_LINK_LIBRARIES(actc)

